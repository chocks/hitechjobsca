# --- Secrets / environment ---
.env
.env.local
*.local

# --- Node (apps/web) ---
node_modules/
npm-debug.log*
yarn-error.log*

# --- Java / Spring (apps/api) ---
target/
build/
out/
*.class
.gradle/

# --- IDE ---
.idea/
.vscode/
*.iml
*.code-workspace

# --- OS ---
.DS_Store
Thumbs.db

# NOTE: apps/web/bundle.js is intentionally tracked -- it is the prebuilt
# frontend artifact (the 2020 Webpack 1 toolchain can't be rebuilt on modern
# Node). It will be removed from version control once the build is modernized.
